info:
  name: Start Focus Session
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/api/focus
  body:
    type: json
    data: |-
      {
        "action": "start"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
