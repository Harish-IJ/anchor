info:
  name: Gmail Auth Connect (Work)
  type: http
  seq: 22

http:
  method: GET
  url: http://localhost:3000/api/auth/gmail/connect?account=work
  headers: {}
  auth: inherit

docs: Open this URL in a browser to connect your WORK Gmail account.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 5
