info:
  name: Google Auth - Status
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:3000/api/auth/google/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
