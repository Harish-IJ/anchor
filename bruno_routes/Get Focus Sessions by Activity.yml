info:
  name: Get Focus Sessions by Activity
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3000/api/focus?activity_id=REPLACE_WITH_ACTIVITY_ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
