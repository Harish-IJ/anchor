info:
  name: List Notion Sources
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:3000/api/notion/sources
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
